# Supermodel NVRAM sample campaign. Paths support ~ and environment variables.

[retroarch]
executable = "~/Retro/Apps/RetroArch/Nightly/RetroArch (ARM).app/Contents/MacOS/RetroArch"
core = "~/Library/Application Support/RetroArch/cores/supermodel_libretro.dylib"
rom_dir = "${SUPERMODEL_ROM_DIR}"
system_assets = "../Config"
base_config = "~/Library/Application Support/RetroArch/config/retroarch.cfg"

[campaign]
game = "daytona2"
standard_srm = "~/Documents/RetroArch/model3-nvram-analysis/daytona2/game-base.srm"
output_dir = "~/Documents/RetroArch/model3-nvram-analysis/daytona2/generated"

# Common timings, in real seconds.
startup_wait = 5.0
button_hold = 0.06
button_hold_long = 0.12
default_wait = 0.18
settle_before_close = 0.5
close_gap = 0.7
shutdown_timeout = 15.0
window_width = 496
window_height = 384

# A macro may be a comma-separated string or an array of strings. Macros can
# reference other macros; *N repeats a button or macro. CLOSE performs the
# exact clean shutdown sequence Esc, wait, Esc on this RetroArch process only.
[macros]
EnterServ = "L3, WAIT(0.7)"
OpenGameAssignments = "R3*8, L3, WAIT(0.5)"
ExitServfromGame = "R3*5, L3, WAIT(3.0)"
SelectDifficultyType = "R3*4"
ExitDifficultyType = "R3*6, L3, WAIT(0.5)"
SelectCabinetType = "R3*3"
ExitCabinetType = "R3*7, L3, WAIT(0.5)"
SelectLinkType = "R3*1"
ExitLinkType = "R3*9, L3, WAIT(0.5)"
ExitLinkType01 = "R3*4, L3, WAIT(0.5)"
ExitLinkType02 = "R3*2, L3, WAIT(0.5)"
ExitLinkType03 = "R3*8, L3, WAIT(0.5)"
ExitLinkId = "R3*8, L3, WAIT(0.5)"
OpenCountryAssignments = "HOLD(START), RIGHT, RIGHT, DOWN, LEFT, UP, LEFT, DOWN, RELEASE(START), WAIT(0.5)"
ExitServfromCountry = "L3, WAIT(3.0)"
SelectCountryType = "R3, WAIT(0.5)"
ExitCountryType = "R3, L3, WAIT(0.5)"
Close = "CLOSE"

# Starting from the immutable DELUXE standard above, one press on Cabinet Type
# selects TWIN. The script copies the standard again before every other sample.
[[samples]]
suffix = "difficulty-00-normal"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*0, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-01-hard"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*1, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-02-harder"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*2, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-03-easy"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*3, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "country-00-japan"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*0, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-01-usa"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*1, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-02-export"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*2, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-03-australia"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*3, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-04-korea"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*4, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "cabinet-00-twin"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectCabinetType, L3*0, WAIT(0.5)",
  "@ExitCabinetType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "cabinet-01-deluxe"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectCabinetType, L3*1, WAIT(0.5)",
  "@ExitCabinetType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-00-master"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-01-slave"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*1, WAIT(0.5)",
  "@ExitLinkType01",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-02-live"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*2, WAIT(0.5)",
  "@ExitLinkType02",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-03-single"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*3, WAIT(0.5)",
  "@ExitLinkType03",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-00-car-01"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*0, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-01-car-02"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*1, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-02-car-03"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*2, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-03-car-04"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*3, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-04-car-05"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*4, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-05-car-06"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*5, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-06-car-07"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*6, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-07-car-08"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*7, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-08-car-09"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*8, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-09-car-10"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*9, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-10-car-11"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*10, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-11-car-12"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*11, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-12-car-13"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*12, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-13-car-14"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*13, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-14-car-15"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*14, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-id-15-car-16"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5), R3, L3*15, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromGame",
  "@Close",
]

# Add further samples in the same form. WAIT(seconds) can appear anywhere.
# If @Close/CLOSE is omitted, the script automatically closes RetroArch cleanly.
#
# [[samples]]
# suffix = "another-setting"
# sequence = "@EnterServ, R3*4, L3, WAIT(1.0), @Close"
