# Supermodel NVRAM sample campaign. Paths support ~ and environment variables.

[retroarch]
executable = "~/Retro/Apps/RetroArch/Nightly/RetroArch (ARM).app/Contents/MacOS/RetroArch"
core = "~/Library/Application Support/RetroArch/cores/supermodel_libretro.dylib"
rom_dir = "${SUPERMODEL_ROM_DIR}"
system_assets = "../Config"
base_config = "~/Library/Application Support/RetroArch/config/retroarch.cfg"

[campaign]
game = "srally2"
standard_srm = "~/Documents/RetroArch/model3-nvram-analysis/srally2/game-base.srm"
output_dir = "~/Documents/RetroArch/model3-nvram-analysis/srally2/generated"

# Common timings, in real seconds.
startup_wait = 5.0
button_hold = 0.06
button_hold_long = 4.5
default_wait = 0.18
settle_before_close = 0.5
close_gap = 0.7
shutdown_timeout = 15.0
window_width = 496
window_height = 384

# A macro may be a comma-separated string or an array of strings. Macros can
# reference other macros; *N repeats a button or macro. CLOSE performs the
# exact clean shutdown sequence Esc, wait, Esc on this RetroArch process only.
[macros]
EnterServ = "L3, WAIT(0.7)"
OpenGameAssignments = "R3*9, L3, WAIT(0.5)"
SelectCabinetType = "R3*8"
ExitCabinetType = "R3*2, L3, WAIT(0.5)"
SelectLinkType = "R3*9"
ExitLinkType = "R3, L3, WAIT(0.5)"
ExitServfromGame = "R3*3, L3, WAIT(3.0)"
OpenCountryAssignments = "SHORT(R3)*4, LONG(R3)*2, SHORT(R3)*2, LONG(R3)*1"
SelectCountryType = ""
ExitCountryType = "L3, WAIT(0.5)"
ExitServfromCountry = "R3*3, L3, WAIT(3.0)"
SelectDifficultyType = "R3*2"
ExitDifficultyType = "R3*8, L3, WAIT(0.5)"
SelectDefaultViewType = "R3*4"
ExitDefaultViewType = "R3*6, L3, WAIT(0.5)"
Close = "CLOSE"

# Starting from the immutable DELUXE standard above, one press on Cabinet Type
# selects TWIN. The script copies the standard again before every other sample.
[[samples]]
suffix = "difficulty-00-normal"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*0, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-01-hard"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*1, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-02-very-hard"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*2, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-03-very-easy"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*3, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-04-easy"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*4, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "cabinet-00-deluxe"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectCabinetType, L3*0, WAIT(0.5)",
  "@ExitCabinetType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "cabinet-01-twin"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectCabinetType, L3*1, WAIT(0.5)",
  "@ExitCabinetType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-00-stand-alone"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-01-master-car-01"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*1, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-02-slave-car-02"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*2, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-03-slave-car-03"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*3, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-04-slave-car-04"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*4, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-05-relay"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectLinkType, L3*5, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "country-00-export"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, R3*0, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-01-japan"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, R3*1, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-02-usa"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, R3*2, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-03-australia"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, R3*3, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "default-view-00-driver"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDefaultViewType, L3*0, WAIT(0.5)",
  "@ExitDefaultViewType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "default-view-01-behind"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDefaultViewType, L3*1, WAIT(0.5)",
  "@ExitDefaultViewType",
  "@ExitServfromGame",
  "@Close",
]


# Add further samples in the same form. WAIT(seconds) can appear anywhere.
# If @Close/CLOSE is omitted, the script automatically closes RetroArch cleanly.
#
# [[samples]]
# suffix = "another-setting"
# sequence = "@EnterServ, R3*4, L3, WAIT(1.0), @Close"
