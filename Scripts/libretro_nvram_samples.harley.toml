# Supermodel NVRAM sample campaign. Paths support ~ and environment variables.

[retroarch]
executable = "~/Retro/Apps/RetroArch/Nightly/RetroArch (ARM).app/Contents/MacOS/RetroArch"
core = "~/Library/Application Support/RetroArch/cores/supermodel_libretro.dylib"
rom_dir = "${SUPERMODEL_ROM_DIR}"
system_assets = "../Config"
base_config = "~/Library/Application Support/RetroArch/config/retroarch.cfg"

[campaign]
game = "harley"
standard_srm = "~/Documents/RetroArch/model3-nvram-analysis/harley/game-base.srm"
output_dir = "~/Documents/RetroArch/model3-nvram-analysis/harley/generated"

# Common timings, in real seconds.
startup_wait = 5.0
button_hold = 0.06
button_hold_long = 0.12
default_wait = 0.2
settle_before_close = 0.5
close_gap = 0.7
shutdown_timeout = 15.0
window_width = 496
window_height = 384

# A macro may be a comma-separated string or an array of strings. Macros can
# reference other macros; *N repeats a button or macro. CLOSE performs the
# exact clean shutdown sequence Esc, wait, Esc on this RetroArch process only.
[macros]
EnterServ = "L3, WAIT(0.7)"
OpenGameAssignments = "R3*7, L3, WAIT(0.5)"
ExitServfromGame = "R3*7, L3, WAIT(4.0)"
SelectDifficultyType = "R3*1"
ExitDifficultyType = "R3*7, L3, WAIT(0.5)"
SelectCabinetType = "R3*3"
ExitCabinetType = "R3*5, L3, WAIT(0.5)"
SelectLinkComm = "R3*1"
ExitLinkComm = "R3*1, L3, WAIT(0.5)"
ExitLinkComm01 = "R3*3, L3, WAIT(0.5)"
SelectLinkType = "R3*1, L3*1, R3*1"
ExitLinkType = "R3*2, L3, WAIT(0.5)"
SelectLinkId = "R3*1, L3*1, R3*2"
ExitLinkId = "R3*1, L3, WAIT(0.5)"
OpenCountryAssignments = "R3*7, L3, WAIT(0.5)"
ExitServfromCountry = "R3*7, L3, WAIT(4.0)"
SelectCountryType = "R*2, L*2, UP, A, UP, A, R3"
ExitCountryType = "R3*8, L3, WAIT(0.5)"
OpenNetAssignments = "R3*9, L3, WAIT(0.5)"
ExitServfromNet = "R3*5, L3, WAIT(4.0)"
Close = "CLOSE"

# Starting from the immutable DELUXE standard above, one press on Cabinet Type
# selects TWIN. The script copies the standard again before every other sample.
[[samples]]
suffix = "difficulty-00-normal"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*0, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-01-plus-05-sec"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*1, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-02-plus-10-sec"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*2, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-03-plus-15-sec"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*3, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-04-plus-20-sec"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*4, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-05-minus-10-sec"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*5, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "difficulty-06-minus-05-sec"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectDifficultyType, L3*6, WAIT(0.5)",
  "@ExitDifficultyType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "country-00-export"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*0, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-01-korea"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*1, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-02-australia"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*2, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-03-japan"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*3, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "country-04-usa"
sequence = [
  "@EnterServ",
  "@OpenCountryAssignments",
  "@SelectCountryType, L3*4, WAIT(0.5)",
  "@ExitCountryType",
  "@ExitServfromCountry",
  "@Close",
]

[[samples]]
suffix = "cabinet-00-deluxe"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectCabinetType, L3*0, WAIT(0.5)",
  "@ExitCabinetType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "cabinet-01-standard"
sequence = [
  "@EnterServ",
  "@OpenGameAssignments",
  "@SelectCabinetType, L3*1, WAIT(0.5)",
  "@ExitCabinetType",
  "@ExitServfromGame",
  "@Close",
]

[[samples]]
suffix = "link-00-stand-alone"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkComm, L3*0, WAIT(0.5)",
  "@ExitLinkComm",
  "@ExitServfromNet",
  "@Close",
]

# [[samples]]
# suffix = "link-comm-01-network"
# sequence = [
#   "@EnterServ",
#   "@OpenNetAssignments",
#   "@SelectLinkComm, L3*1, WAIT(0.5)",
#   "@ExitLinkComm01",
#   "@ExitServfromNet",
#   "@Close",
# ]

[[samples]]
suffix = "link-01-master"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkType, L3*0, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromNet",
  "@Close",
]

[[samples]]
suffix = "link-02-slave"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkType, L3*1, WAIT(0.5)",
  "@ExitLinkType",
  "@ExitServfromNet",
  "@Close",
]

[[samples]]
suffix = "link-id-00-cabinet-01"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkId, L3*0, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromNet",
  "@Close",
]

[[samples]]
suffix = "link-id-01-cabinet-02"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkId, L3*1, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromNet",
  "@Close",
]

[[samples]]
suffix = "link-id-02-cabinet-03"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkId, L3*2, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromNet",
  "@Close",
]

[[samples]]
suffix = "link-id-03-cabinet-04"
sequence = [
  "@EnterServ",
  "@OpenNetAssignments",
  "@SelectLinkId, L3*3, WAIT(0.5)",
  "@ExitLinkId",
  "@ExitServfromNet",
  "@Close",
]

# Add further samples in the same form. WAIT(seconds) can appear anywhere.
# If @Close/CLOSE is omitted, the script automatically closes RetroArch cleanly.
#
# [[samples]]
# suffix = "another-setting"
# sequence = "@EnterServ, R3*4, L3, WAIT(1.0), @Close"
